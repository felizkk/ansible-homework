---
- name: Create Server Instance for {{ instance_name }}
  os_server:
    cloud: "{{ cloud_name }}"
    name: "{{ instances.name }}"
    image: rhel-guest
    meta: "group={{ instances.group }},deployment_name={{ instances.deployment }}"
    flavor: m2.small
    security_groups: "{{ instances.security_group_name }}"
    key_name: ansible_ssh
    wait: yes
    nics:
      - net-name: "{{ int_network_name }}"
    userdata: |
      #!/bin/bash
      curl -o /tmp/openstack.pub http://www.opentlc.com/download/ansible_bootcamp/openstack_keys/openstack.pub
      cat /tmp/openstack.pub >> /home/cloud-user/.ssh/authorized_keys
      curl -o /tmp/internal.repo http://www.opentlc.com/download/ansible_bootcamp/repo/internal.repo
      cp /tmp/internal.repo /etc/yum.repos.d/internal.repo
  when: instances.name == instance_name
  register: instanceip
  tags:
    - osp-instances

- name: Add floating IP to {{ instance_name }} Instance
  os_floating_ip:
    cloud: "{{ cloud_name }}"
    state: present
    reuse: yes
    server: "{{ instances.name }}"
    network: "{{ ext_network_name }}"
    wait: true
    timeout: 180
  when: instances.name == instance_name
  register: instancefloatingip
  tags:
    - osp-instances

- name: Show Instances public IP
  debug: var=instancefloatingip.floating_ip.floating_ip_address
  tags:
    - osp-instances

- name: Wait for "{{ instance_name }}" to be available
  wait_for:
    host: "{{ instancefloatingip.floating_ip.floating_ip_address }}"
    port: 22
    search_regex: OpenSSH
    timeout: 600
  delegate_to: "{{ inventory_hostname }}"
  tags:
    - osp-instances
...
